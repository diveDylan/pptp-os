{
  "name": "pptp",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "package": "electron-packager . 'pptp' --platform=darwin --arch=x64  --out=./dist --asar --app-version=1.0.0 --overwrite --ignore=\"(dist|src|docs|.gitignore|LICENSE|README.md|webpack.config)\"",
    "package:mac": "electron-packager . 'pptp' --platform=mas --arch=x64  --out=./dist --asar --app-version=1.0.0 --overwrite --ignore=\"(dist|src|docs|.gitignore|LICENSE|README.md|webpack.config)\"",
    "start": "electron ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^9.4.0",
    "electron-packager": "^14.1.1",
    "enpm": "^0.3.0",
    "install": "^0.13.0"
  },
  "dependencies": {
    "lowdb": "^1.0.0",
    "sudo-js": "^1.0.2"
  }
}
